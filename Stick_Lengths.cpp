#include <bits/stdc++.h>
using namespace std;

int32_t main () {
  ios_base::sync_with_stdio (0);
  cin.tie (0);
  int n;
  cin >> n;
  long long p[n];
  long long gotta_add[n] = { 0 };
  long long gotta_cut[n] = { 0 };
  for (int i = 0; i < n; i++) {
    cin >> p[i];
  }
  sort (p, p + n);
  for (int i = 0; i < n-1; i++) {

  }
  return 0;
}